need "alcl/compiler_testing"
need "io"

func main(): Int32 {
    if (a == b) {
        println("a == b")
    } else if (c == d) {
        println("c == d")
    } else {
        println("a != b and c != d")
    }
}