func add(a, b, c) {
    return a + b + c * 1.0;
}

func mul(a, b, c) {
    return a * b * c;
}

func main(u) {
    var x = null;
    var i = mul(1, 2, 3)
    var o = mul(1, i, 3)
    var y = add(i, 2, o) - u;
    x = y;
    x = null;
    x = 3;
    x = null;

    var z = null;
    var q = true;
    z = 1;
    x = z;

    if (x > z) {
        return q ? x : z = 3;
    } else {
        return q ? z = 3 : x;
    }
}
