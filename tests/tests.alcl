// used for test summary
need "alcl/compiler_testing"

// Runtime tests
need "test_math"
need "test_while"
need "test_comparisons"
need "test_for"
need "test_ternary"

// Typing tests
need "typing_func_recursive_multi_call"
need "typing_func_recursive_self_call"
need "typing_null_type_resolution"
need "typing_null_type_resolution_multiple"
need "typing_var_to_var_assignment"

// run tests
func main(): Int32 {
    test_math();
    test_while();
    test_comparisons();
    test_for();
    test_ternary();

    return alcl_test_summary();
}