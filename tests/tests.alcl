// used for test summary
need "alcl/compiler_testing"

// tests
need "tests/test_math"
need "tests/test_while"
need "tests/test_comparisons"
need "tests/test_for"
need "tests/test_ternary"
need "tests/test_typing"

// run tests
func main(): Int32 {
    test_math();
    test_while();
    test_comparisons();
    test_for();
    test_ternary();

    return alcl_test_summary();
}